# ============================================
# AIRWAVE MISSION CONTROL - ENVIRONMENT VARIABLES
# ============================================

# Security Configuration
ADMIN_TOKEN=your_secure_admin_token_here
ALLOWED_ORIGINS=http://localhost:8501,http://localhost:5773
NODE_ENV=development
LOG_LEVEL=info

# Airframes.io Configuration
AIRFRAMES_API_KEY=your_airframes_api_key_here

# EAM.watch Configuration
# Emergency Action Message API for monitoring strategic communications
EAM_WATCH_API_TOKEN=your_eam_watch_token_here
EAM_WATCH_BASE_URL=https://api.eam.watch

# Twitter/X API Configuration (Optional - for posting videos)
# Obtain credentials from https://developer.twitter.com/
# Required OAuth 2.0 scopes: tweet.read, tweet.write, media.write, users.read
TWITTER_API_KEY=your_api_key_here
TWITTER_API_SECRET=your_api_secret_here
TWITTER_ACCESS_TOKEN=your_access_token_here
TWITTER_ACCESS_SECRET=your_access_secret_here

# Video Rendering Configuration
VIDEO_OUTPUT_DIR=backend/data/videos
VIDEO_CLEANUP_HOURS=24
VIDEO_DEFAULT_FPS=30
VIDEO_DEFAULT_DURATION=10

# Backend Server Configuration
PORT=5773

# TAR1090 Configuration (Optional - for ADS-B integration)
TAR1090_URL=http://192.168.1.120/skyaware/data/aircraft.json
TAR1090_POLL_INTERVAL=2000

# ADS-B Exchange Configuration (Optional - for global ADS-B data)
# Obtain API key from https://www.adsbexchange.com/data/ (Enterprise/API Lite customers)
# API key should be in UUID format (e.g., 12345678-1234-1234-1234-123456789abc)
# Maximum distance is 100 NM per API limits
# Poll interval in milliseconds (minimum 5000ms recommended to avoid rate limiting)
# This is optional - the app works without it
ADSBEXCHANGE_API_KEY=your_api_key_here
ADSBEXCHANGE_API_URL=https://adsbexchange.com/api/aircraft
ADSBEXCHANGE_DEFAULT_LAT=55.8642
ADSBEXCHANGE_DEFAULT_LON=-4.2518
ADSBEXCHANGE_DEFAULT_DIST=10
ADSBEXCHANGE_POLL_INTERVAL=10000

# Whisper Configuration (for ATC transcription)
WHISPER_SERVER_URL=http://localhost:8080

# Database Configuration
DATABASE_PATH=backend/data/airwave.db

# Aircraft Photo Service Configuration
# JetAPI provides free aircraft photos from JetPhotos and FlightRadar24
# No API key required - photos are fetched from public sources
JETAPI_BASE_URL=https://www.jetapi.dev/api
JETAPI_DEFAULT_PHOTO_COUNT=5
JETAPI_CACHE_TTL_DAYS=7
JETAPI_RATE_LIMIT_MS=1000
JETAPI_PREFETCH_INTERVAL_MINUTES=30

# ============================================
# NOTE: Twitter integration is optional
# The app will work without Twitter credentials
# Just won't be able to post videos to Twitter/X
# ============================================

